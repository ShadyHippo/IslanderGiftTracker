# --- Build artifacts (regenerate with `make build-db`) ---
reference.db
img/
reference.v*.db.gz
cache/
images-missed.txt
build.log

# --- Pristine full-data backup (never trimmed; restore point, see AGENTS.md) ---
pristine/

# --- Server runtime data: secrets + user data, never commit ---
server/data/
server/bin/
server/deploy/ref/
dev-data/

# --- Client build ---
client/node_modules/
client/dist/
client/.pnpm-store/

# --- E2E artifacts (generated by `make e2e` in the playwright container) ---
tests/e2e/node_modules/
tests/e2e/smoke.png

# --- Misc ---
__pycache__/
*.pyc
.env
.vite/

# --- Agent tooling (machine-local, not project source) ---
.maki/
cont_maki.sh
images/
reference.db.tmp
reference.v*.db.gz.tmp

# --- Committed build/test artifacts (regenerated locally) ---
server/acnh
tests/e2e/*.png
